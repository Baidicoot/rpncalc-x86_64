(# library arith #)
(# include arith #)
(# include bool #)

(export ++; 1 +)
(export --; 1 -)

(export !; x ->
    1
    '(->x -- ! x *)
    x 0 == ?:)

(export >=; a b -> a b > a b == or)
(export <=; '> 'not)
(export <; a b -> a b <= a b == not and)